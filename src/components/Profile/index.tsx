import React from 'react';

import { ProfileHandles } from './types';

const Profile: React.FC<ProfileHandles> = ({
  user,
  card,
  maincard,
}) => (
  <div className="">
    <img src="" alt="" />
  </div>
);
